{
  "name": "nasa-project-api",
  "version": "1.0.0",
  "description": "NASA Mission control API",
  "main": "src/server.js",
  "scripts": {
    "test": "jest --detectOpenHandles",
    "test-watch": "jest --watch",
    "start": "SET PORT=8000 && node --watch src/server.js",
    "cluster": "pm2 start src/server.js -i max -n nasa-server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "csv-parse": "^5.5.6",
    "express": "^4.19.2",
    "mongoose": "^8.7.3",
    "morgan": "^1.10.0"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
