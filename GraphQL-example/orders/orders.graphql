type Query {
  orders: [Order]
}
type Review {
  rating: Int!
  comment: String
}
type Order {
  id: ID!
  date: String!
  subtotal: Float!
  items: [OrderItem]
}
type OrderItem {
  product: Product!
  quantity: Int!
}
